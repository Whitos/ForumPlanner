{% extends 'base.html.twig' %}

{% block title %}Edit Forum{% endblock %}

{% block body %}
    <h1 class="text-2xl font-bold mb-5">Edit Forum</h1>

    <div class="bg-white shadow-md rounded-lg p-6 mb-5">
        {{ include('forum/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="mt-5">
        <a href="{{ path('app_forum_index') }}" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">Back to list</a>
    </div>

    <div class="mt-5">
        {{ include('forum/_delete_form.html.twig') }}
    </div>
{% endblock %}
